{% extends 'base.html.twig' %}
{% block menu %}Liste des WooCommerceApis{% endblock %}
{% block body %}
<div class="col-12">
        <div class="card card-primary col-12">
            <div class="card-header">
                <ul class="nav">
                    <li class="nav-item">
                            Liste des WooCommerceApis
                    </li>
                    <li class="nav-item ml-auto mr-2">
                        <a href="{{ makeUrl('TijaraShop/woocommerceapi/new') }}" class="btn btn-sm btn-success">Ajouter WooCommerceApi</a>
                    </li>
                </ul>
            </div>
            <div class="card-body table-responsive">
                <table class="table " id="myTable">
                    <thead class="thead-dark">
                    <th>id </th>
<th>key </th>
<th>secret </th>

                    <th>Action</th>
                    </thead>
                    <tbdody>
                        {% for WooCommerceApi in WooCommerceApis %}
                        <tr>
                           <td>{{ WooCommerceApi.id }}</td>
<td>{{ WooCommerceApi.key }}</td>
<td>{{ WooCommerceApi.secret }}</td>


                           <td>
                                <a href="{{ makeUrl('TijaraShop/woocommerceapi/read') }}&id={{ WooCommerceApi.id }}">
                                        <button class="btn btn-success" data-toggle="tooltip" data-placement="top"
                                                title="Voir">
                                            <i class="fa fa-eye"></i>
                                        </button>
                                    </a>

                                <div class="float-right">
                                    <a href="{{ makeUrl('TijaraShop/woocommerceapi/edit') }}&id={{ WooCommerceApi.id }}">
                                        <button class="btn btn-warning" data-toggle="tooltip" data-placement="top"
                                            title="Editer">
                                            <i class="fa fa-edit"></i>
                                        </button>
                                    </a>

                                    <a href="{{ makeUrl('TijaraShop/woocommerceapi/delete') }}&id={{ WooCommerceApi.id }}">
                                        <button class="btn  btn-danger" data-toggle="tooltip" data-placement="top"
                                            title="Supprimer">
                                            <i class="fa fa-trash"></i>
                                        </button>
                                    </a>
                                </div>
                           </td>
                        </tr>

                        {% endfor %}
                    </tbdody>
                </table>
                <script>
                    $(document).ready(function () {
                        $('#myTable').dataTable({
                            "searching": false
                        });
                    });
                </script>
            </div>
        </div>
    </div>
{% endblock %}